# Project Explorer Candidates

Alternatives in `../project_explorer_icon_review.html`. Selected option A (Project Collection)
now uses the production `ui/shell/tab-type-icons/project_settings.svg` directly; the other options remain preview-only.

Each SVG owns its artwork. The adjacent stylesheet applies it as a current-color mask.
Follow the production tab set: 16-unit canvas, 1.25-unit outline, round caps and joins,
quarter-unit coordinates, and roughly five elements or fewer. Keep ink inside the canvas.
Add a standalone SVG, a stylesheet mapping, and an entry in the review page to add an option.
