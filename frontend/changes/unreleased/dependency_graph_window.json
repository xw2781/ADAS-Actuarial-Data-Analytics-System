{
  "type": "feature",
  "scope": "project instance",
  "audience": "user",
  "summary": "Added a Dependency Graph window that draws every dataset and method of the selected reserving class and how they feed each other.",
  "details": [
    "Open it from the new graph icon in the dataset toolbar; it is a nested window like Manage Excel Links, pinned to the reserving class it was opened on.",
    "Click a box to light up what it is computed from and what depends on it, and keep it lit until you click elsewhere; double-click a box to open the dataset in Dataset Viewer or a method output in its method page.",
    "Hover a box for two small ports on its edges: the left one lists its precedents, the right one its dependents, and clicking a name scrolls the diagram to that box.",
    "Datasets nothing depends on are hidden by default; tick Show all to see them. The diagram scrolls inside ArcRho scrollbars, and dragging the background or scrolling the wheel steers it.",
    "The diagram redraws on its own after a save, delete, or import changes the class."
  ]
}
